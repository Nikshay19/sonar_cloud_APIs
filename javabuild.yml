name: Build
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Build and analyze
        env:
          GITHUB_TOKEN: 1234rthjrthtr556  # This is some junk value  - this is needed so  dont ignore it
          SONAR_TOKEN: 5f823c56a6fb3dad72af67881b48e2997ba46b33 #Add it to keys.json and get it from there
        run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=neojarvis-testing_b733dda5-4189-4a0a-9310-98cb0433b39d -Dsonar.login=5f823c56a6fb3dad72af67881b48e2997ba46b33
